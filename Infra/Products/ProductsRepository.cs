using MaskShop.Data.Products;
using MaskShop.Infra.Common;

namespace MaskShop.Infra.Products
{
    //public sealed class ProductsRepository : UniqueEntityRepository<IProduct, ProductData>
    //{
    //    public ProductsRepository(ProductDbContext c = null) : base(c, c?.Products) { }

    //    protected internal override IProduct toDomainObject(ProductData d) => ProductFactory.Create(d);
    //}
}
