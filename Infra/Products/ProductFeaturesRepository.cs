using MaskShop.Data.Products;
using MaskShop.Infra.Common;

namespace MaskShop.Infra.Products
{
    //public sealed class ProductFeaturesRepository : UniqueEntityRepository<IProductFeature, ProductFeatureData>
    //{
    //    public ProductFeaturesRepository(ProductDbContext c = null) : base(c, c?.ProductFeatures) { }

    //    protected internal override IProductFeature toDomainObject(ProductFeatureData d) => ProductFeatureFactory.Create(d);
    //}
}
