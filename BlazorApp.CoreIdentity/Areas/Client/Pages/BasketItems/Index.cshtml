@page
@using MaskShop.PagesCore.Common.Aids
@using MaskShop.PagesCore.Common.Extensions
@model MaskShop.PagesCore.ShopClient.Orders.BasketItemsClientPage;

@{
    Layout = "_BasketItemsIndex";
    var page = Model.PageUrl;
}

@section Create {
    <h4>
        @Html.Link(string.Empty, new Link("Ostma", Model.CreateUrl))
        @Html.Link(string.Empty, new Link("Maksma", Model.OrdersUrl))
    </h4>}

@Html.ShowTable(Model)
